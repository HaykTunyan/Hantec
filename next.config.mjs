/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        defaultLocale: 'en',
        locales: ['en', 'fr'],
    },
    reactStrictMode: false,
};

export default nextConfig;
